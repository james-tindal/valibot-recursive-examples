
# Problem description

When `declaration: true` is set in the importing package's tsconfig

`RecursiveSelfSchema`, `RecursiveArray`, and `RecursiveMarkerSymbol` are required to be public so they can be referenced in the built .d.ts files.

See `src/demonstration.ts`
